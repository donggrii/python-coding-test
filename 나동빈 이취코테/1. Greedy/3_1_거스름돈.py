# 화폐의 종류(K)만큼 반복을 수행(시간복잡도 O(K))
# 시간복잡도는 동전의 총 종류에만 영향을 받고, 금액의 크기와는 무관함

n = 1260
cnt = 0

# 큰 단위의 화폐부터 차례대로 확인
coin_types = [500, 100, 50, 10]

for coin in coin_types:
    cnt += n // coin    # 해당 화폐로 거슬러 줄 수 있는 동전의 개수 세기
    n %= coin           # 해당 화폐로 거슬러 준 뒤 나머지

print(cnt)   # 6